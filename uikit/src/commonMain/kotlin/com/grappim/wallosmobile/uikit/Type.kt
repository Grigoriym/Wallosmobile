package com.grappim.wallosmobile.uikit

import androidx.compose.material3.Typography

/**
 * Material 3's default type scale, untouched. This exists as the single edit point for the day
 * the app ships a font of its own — `MaterialTheme` is handed this and never the default.
 */
internal val WallosTypography = Typography()
