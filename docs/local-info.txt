Deliberately in the repo, and safe: this is a LAN-only Wallos container that is not reachable
from outside the machine it runs on. Nothing here is a credential for anything else.

Use this instance for the on-device Verify lines, not a public demo — https://demo.wallosapp.com
has a broken profile.php (PHP fatal: no such table `uploaded_avatars`), so the login bridge finds
no API key to scrape there and Path A can never go green against it.

From the emulator, `localhost` is the emulator itself — reach the host as http://10.0.2.2:8282.

creds for local instance:
http://localhost:8282/
gregorz
Unequal2-Reawake9-Vessel3-Delouse8-Resonate9


================================================================================
The 2FA instance — port 8283, off by default
================================================================================

A second container for anything that needs two-factor auth, so no step ever has to enable 2FA on
the real account. It is a *copy* of the real instance's database taken 2026-08-03, so it has the
same username, the same password as above, and the same 35 subscriptions — but its own API key,
regenerated on the copy so a disposable container isn't holding a working credential for the live
one. Nothing in it writes back to /home/gregory/data/wallos.

  data + compose:   /home/gregory/data/wallos-totp/
  start:            docker compose -f /home/gregory/data/wallos-totp/compose.yaml up -d
  stop:             docker compose -f /home/gregory/data/wallos-totp/compose.yaml stop
  from the host:    http://localhost:8283/
  from the emulator: http://10.0.2.2:8283

It is `restart: "no"` on purpose — it stays off between sessions and survives a reboot stopped.
Give it ~10s after `up -d` before the first request.

  username          gregorz
  password          (the same one above)
  TOTP secret       JBSWY3DPEHPK3PXP        (Base32, 30s period — a published test vector)
  current code      python3 scripts/totp-code.py
  backup codes      bbbbccccddddeeeeffff  ccccddddeeeeffff0000
                    ddddeeeeffff00001111  eeeeffff000011112222
                    (aaaabbbbccccddddeeee was spent proving they work — Wallos deletes a backup
                     code as it consumes it, and 20 hex chars is the real format)

Re-seed the TOTP row if the codes run out or the account gets into a bad state:

  docker exec wallos-totp php -r '$d=new SQLite3("/var/www/html/db/wallos.db");
    $d->exec("DELETE FROM totp WHERE user_id = 1");
    $s=$d->prepare("INSERT INTO totp (user_id,totp_secret,backup_codes,last_totp_used)
                    VALUES (1,:s,:b,0)");
    $s->bindValue(":s","JBSWY3DPEHPK3PXP"); $s->bindValue(":b",json_encode(
      ["aaaabbbbccccddddeeee","bbbbccccddddeeeeffff","ccccddddeeeeffff0000",
       "ddddeeeeffff00001111","eeeeffff000011112222"])); $s->execute();
    $d->exec("UPDATE user SET totp_enabled = 1 WHERE id = 1");'

Turning 2FA off on this instance (to test the plain path against the same data) is
`UPDATE user SET totp_enabled = 0 WHERE id = 1` in the same way. To force the "session expired"
branch mid-flow: docker exec wallos-totp sh -c 'rm -f /tmp/sess_*'


================================================================================
The currency-conversion instance — wallos-scratch, port 8284, stopped by default
================================================================================

The live instance is single-currency (all rows EUR, conversion off, rates never fetched), so
nothing about currency conversion is visible there — neither the working case nor the failing one.
This container is a copy of the live database with 8 of the 35 rows moved to `currency_id = 2`
(USD) and the copied API key rotated, so it isn't holding a working credential for the real
instance.

  start:              docker start wallos-scratch
  from the emulator:  http://10.0.2.2:8284
  get the API key:    docker exec wallos-scratch php -r '$d=new
                       SQLite3("/var/www/html/db/wallos.db"); $r=$d->query("SELECT api_key FROM
                       user WHERE id=1"); echo $r->fetchArray()[0];'

That key is what the app's "I have an API key" path wants (no web-login bridge needed). Logging
into a *different* server than whatever is already stored needs
`adb shell pm clear com.grappim.wallosmobile.debug` first, or the app opens on the previous
server's cached rows.

**Since 5.4 it converts**: `currencies.rate = 1.17` for USD, `settings.convert_currency = 1`, and
a `last_exchange_update` row — the flag the server actually gates on, and the one the app can only
infer from a rate that isn't `1`. So the 8 USD rows come back divided by 1.17 with `currency_id`
still `2`. Three `UPDATE`s undo it if a step needs the *unconverted* state instead:

  docker exec wallos-scratch php -r '$d=new SQLite3("/var/www/html/db/wallos.db");
    $d->exec("UPDATE currencies SET rate = 1.17 WHERE id = 2");
    $d->exec("UPDATE settings SET convert_currency = 1 WHERE user_id = 1");
    $d->exec("INSERT INTO last_exchange_update (date, user_id) VALUES (\"2026-08-05\", 1)");'

Re-copying the live database in is three commands, since the files are uid 82 and the container's
are not a volume:

  docker run --rm -v /home/gregory/data/wallos/db:/src:ro alpine cat /src/wallos.db > wallos.db
  docker cp wallos.db wallos-scratch:/var/www/html/db/wallos.db
  docker exec wallos-scratch chown www-data:www-data /var/www/html/db/wallos.db


================================================================================
Certificate-pinning testing — a throwaway TLS front, built on demand
================================================================================

Wallos speaks plain HTTP, so certificate work needs a TLS front in front of it rather than
touching the Wallos container itself — a throwaway nginx beside it:

  openssl req -x509 -newkey rsa:4096 -sha256 -days 3650 -nodes \
    -keyout ca-key.pem -out ca-cert.pem -subj "/CN=Home Lab Test CA/O=Wallos QA"
  openssl req -newkey rsa:2048 -nodes -keyout server-key.pem -out server.csr \
    -subj "/CN=10.0.2.2/O=Wallos QA"          # CN *and* SAN are the address the app is given
  echo "subjectAltName=IP:10.0.2.2" > san.cnf # no SAN → cert doesn't cover the host → no prompt
  openssl x509 -req -in server.csr -CA ca-cert.pem -CAkey ca-key.pem -CAcreateserial \
    -out server-cert.pem -days 825 -sha256 -extfile san.cnf
  # nginx.conf: listen 8443 ssl; ssl_certificate /certs/server-cert.pem; … proxy_pass http://wallos:80;
  docker run -d --name wallos-tls --network wallos_default -p 8443:8443 \
    -v $(pwd)/certs:/certs:ro -v $(pwd)/nginx.conf:/etc/nginx/conf.d/default.conf:ro nginx:alpine

Then the app's server URL is `https://10.0.2.2:8443`, and **never install the CA on the emulator**
— an untrusted chain is the whole point. `docker rm -f wallos-tls` when done. Regenerating the leaf
and restarting the container is what proves a pin is per-*certificate* and not per-host.

The front's own access log is the proof for anything the Ktor logger can't see (image loads go
through Coil, which has no `Logging` plugin — `adb logcat` says nothing about them):
`docker logs wallos-tls | grep images/uploads/logos`, and the `"ktor-client"` user-agent on a `200`
says the load used the app's pinned engine. Coil caches on disk, so a second run proves nothing
until
`adb shell run-as com.grappim.wallosmobile.debug rm -rf /data/data/com.grappim.wallosmobile.debug/cache/coil3_disk_cache`
— that path, and `pm clear` for everything at once. A failed load is also sticky per request: Coil
does not retry a state that is already `Error`, so after the server comes back the rows keep their
placeholders until they recompose (scroll them off and on).


================================================================================
Any other server-side setting — build a scratch instance the same way
================================================================================

  docker run -d --name wallos-scratch -p 8284:80 bellamy/wallos:latest   # ~10s to boot
  curl -s -o /dev/null -d "username=u&firstname=T&lastname=U&email=u@e.com&password=p&\
  confirm_password=p&main_currency=USD&language=en" http://localhost:8284/registration.php

`registration.php` needs **all** of those fields — a short POST silently re-renders the form and
`login.php` keeps redirecting to `registration.php`, which reads as a broken container. Copying the
real data instead of registering is one `cp -a` of `/home/gregory/data/wallos/db` through a root
container (the files are uid 82), and it is worth it for anything that renders a list. From there
`docker exec <name> php -r '…new SQLite3("/var/www/html/db/wallos.db")…'` writes whatever the step
needs directly, skipping the enrolment endpoints' own CSRF + code dances. PHP sessions are plain
files — `rm -f /tmp/sess_*` inside the container is how you make a live session expire mid-flow.
**Rotate any copied API key**, or a disposable container is holding a working credential for the
real instance.

**OIDC needs no identity provider to test against**: `login.php` only reads
`password_login_disabled` when OIDC is enabled *and* `is_configured`, and `is_configured` is a
non-empty check on seven strings that are never dereferenced unless somebody clicks the button. So
env vars alone produce a real SSO-only instance — `-e OIDC_ENABLED=1 -e
OIDC_DISABLE_PASSWORD_LOGIN=1` plus `OIDC_CLIENT_ID`, `OIDC_CLIENT_SECRET`, `OIDC_AUTH_URL`,
`OIDC_TOKEN_URL`, `OIDC_USERINFO_URL`, `OIDC_REDIRECT_URL` set to anything at all.


================================================================================
The `wallos` MCP — same live instance, and getting a key for `curl`
================================================================================

`mcp__wallos__*` reaches this same real instance (v5.4.2, user `gregorz`, id 1) — read tools are
free to call, but `wallos_add_subscription`/`wallos_update_subscription`/
`wallos_delete_subscription`/`wallos_set_budget` mutate live data, so ask first, every time.

Three limits before leaning on it: it returns the payload **unwrapped** (no `success`/`title`, so
it confirms real values but says nothing about the envelope behaviour `core:api` is built around);
it **adds fields the API doesn't have** (`wallos_list_subscriptions`'s `logo_url` exists nowhere in
the PHP — the server sends the bare `logo` filename, so model DTOs against `curl`, not the MCP);
and it is **one instance at one version** — a field present here may be absent on an older install.

Getting a key for a `curl` against `api/*.php` directly, since the app is the only other thing
that has one:

  curl -s -c /tmp/w.txt -o /dev/null \
    -d "username=gregorz&password=$(grep -A1 '^gregorz$' docs/local-info.txt | tail -1)" \
    http://localhost:8282/login.php
  curl -s -b /tmp/w.txt http://localhost:8282/profile.php | grep -o 'id="apikey"[^>]*'
