#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true
# org.gradle.isolated-projects=true — tried and off for now: Gradle 9.7.0's tightened checks
# break on org.jetbrains.kotlin.multiplatform's KSP task wiring in :core:storage (a third-party
# plugin gap, not fixable here). Re-check on every Gradle/KGP bump. docs/GRADLE_ISOLATED_PROJECTS.md.

#Android
android.nonTransitiveRClass=true
android.useAndroidX=true